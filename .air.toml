root = "."
tmp_dir = "tmp"

[build]
  # air v1.64+에서는 bin 대신 entrypoint 사용 권장
  entrypoint = "./tmp/main"
  cmd = "go build -o ./tmp/main ./cmd/server"
  exclude_dir = ["tmp", ".git", ".claude"]
  include_ext = ["go"]
  send_interrupt = true

[misc]
  clean_on_exit = true
